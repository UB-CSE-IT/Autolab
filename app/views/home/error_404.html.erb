<h2>404 &ndash; Not found</h2>

<h2 style="margin-bottom:0px"><%= @errorMessage %></h2>

<% if flash[:error] %><p><b><%= flash[:error] %></b></p><% end %>

<p><b>Oops!</b> Autolab can't find this page. If you believe there should be something here, please email us at <a href="mailto:<%= Rails.configuration.school['support_email'] %>"><%= Rails.configuration.school['support_email'] %></a> with the page that you were trying to access.</p>

<%= image_tag "DonkeyKong.jpg", width: '200px' %>

<% if @error && current_user.administrator? %>
  <br><br>
  <p>
    <h3>Error Information</h3>
    <pre><%= debug(@error) %></pre>
  </p>
  <p>
    <h3>Stack Dump</h3>
    <pre><%= processStackDump(@error.backtrace).join("<br>").html_safe %></pre>
  </p>
<% end %>

<div>
  <a href="/"><%= Rails.configuration.school['school_short_name'] %> Autolab</a>
  <p>Questions or problems? Please <a href="/home/contact">contact us</a>.</p>
</div>
